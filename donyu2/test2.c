#include <stdio.h>
#include <ctype.h>
#include <time.h>
int main(void) {
	char str[] = { "HIT","OUT" };
	int i, count = 0;
	double avg, x;
	______
		printf("‘Å—¦\n");	scanf("%lf", &avg);
	for (i = 0;i < 9;i++) {
		x =
			if (x < avg) {

				count++;
			}
			else {

			}
			printf("HIT‚Í%d–{", count);
	}
	return 0;
}
